#include <stdio.h>
#include <stdlib.h>:warn("%s");

int main(int argc, char *argv[])
{
  char line[64];
  FILE* input;
  int i_first = 0;
  int i_last = 0;

  //read input
  input = fopen(argv[1], "r");

  while((i_last = getline))
  //get first digit
  //get last digit
  //combine
  //sum and loop back

  return EXIT_SUCCESS;
}
